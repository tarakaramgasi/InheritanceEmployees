using Employees;
namespace PartTimeEmployees
{
    public class PartTimeEmployee:Employee
    {
        public PartTimeEmployee(string name, string ID, int salary) : base(name, ID, salary)
        {

        }
    }
}
